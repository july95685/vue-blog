import Vue from 'vue'
import Vuex from 'vuex'


Vue.use(Vuex)
const state = {
	
}
const actions = {
	// changeFlag({commit}){
 //    commit('Flag')
 //  }
}
const mutations = {
  // add(state,n){
  //   state.num = n;
  // }
  
}

export default new Vuex.Store({
  state,mutations,actions
})
